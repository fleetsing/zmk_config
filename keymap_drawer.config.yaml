# Intentionally minimal for the current Totem layout.
# Add parse_config or draw_config options only when the live keymap needs them.

parse_config:
  # Keep parser overrides explicit; the current keymap renders cleanly with defaults.
  zmk_remove_keycode_prefix: []
